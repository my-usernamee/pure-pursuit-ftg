# config Folder

Primary tweak file:
- `sim.yaml`

Edit here to change:
- map (`map_path`)
- number of agents (`num_agent`)
- spawn positions (`sx`, `sy`, `stheta`, `sx1`, `sy1`, `stheta1`)
- topic/namespace settings
