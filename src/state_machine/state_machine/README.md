# state_machine Module

Exact files:
- `state_machine.py`: state switch thresholds and logic
- `opponent_detector.py`: LiDAR-based opponent detection + temporal tracking
- `obstacle_detector.py`: basic front obstacle threshold detector
