# scripts Folder

Exact scripts to run:

- Start full stack: `scripts/start_racetrack.sh`
- Check status/logs: `scripts/status_racetrack.sh`
- Stop stack: `scripts/stop_racetrack.sh`

If you want to tweak launch parameters (detector thresholds, overtake flags), edit:
- `scripts/start_racetrack.sh`
